id: 62853
name: marinabot-api
environments:
    production:
        domain: api.marinabot.com.br
        database: marinabot-db-production
        cache: marinabot-cache-production
        memory: 1024
        cli-memory: 1024
        runtime: 'php-8.3:al2'
        queues:
            - flows
        build:
            - 'COMPOSER_MIRROR_PATH_REPOS=1 composer install --no-dev'
            - 'php artisan event:cache'
        deploy:
            - 'php artisan migrate --force'
        network:
          ports:
            - 8080:8080
        ssl:
          certificate_arn: arn:aws:acm:us-east-1:975049890705:certificate/8664adc0-e174-4320-9f2a-94bd560320bc
