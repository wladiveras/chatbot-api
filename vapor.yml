id: 62853
name: marinabot-api
environments:
    production:
        domain: api.marinabot.com.br
        database: marinabot
        memory: 1024
        cli-memory: 512
        runtime: 'php-8.3:al2'
        build:
            - 'composer install --no-dev'
            - 'php artisan event:cache'
        deploy:
            - 'php artisan migrate --force'
